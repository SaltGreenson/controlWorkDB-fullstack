type SizeType = {
  maxHeight: string;
  width: string;
};
export const dimensionConfig = {
  small: {
    maxHeight: "44px",
    width: "5rem",
  } as SizeType,
  large: {
    maxHeight: "44px",
    width: "100%",
  } as SizeType,
};
